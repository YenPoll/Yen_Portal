---
import { NAV_LINKS, SITE, SOCIAL_LINKS } from "@utils/constants";

const currentYear = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container footer-inner">
    <div class="footer-top">
      <div class="footer-brand">
        <a href={`${SITE.base}/`} class="footer-logo" aria-label={`${SITE.title}, Home`}>
          <span class="logo-mark" aria-hidden="true">
            <svg width="18" height="18" viewBox="0 0 32 32">
              <path d="M9 8 L16 16 L23 8" stroke="#FFFFFF" stroke-width="2.2" fill="none" stroke-linecap="round" stroke-linejoin="round"></path>
              <line x1="16" y1="16" x2="16" y2="21.5" stroke="#FFFFFF" stroke-width="2.2" stroke-linecap="round"></line>
              <path d="M9 6.3 L10.7 8 L9 9.7 L7.3 8 Z" fill="#C89B3C"></path>
              <path d="M23 6.3 L24.7 8 L23 9.7 L21.3 8 Z" fill="#C89B3C"></path>
              <path d="M16 20.2 L19.3 24 L16 27.8 L12.7 24 Z" fill="#C89B3C"></path>
            </svg>
          </span>
          <span>{SITE.title}</span>
        </a>
        <p class="footer-tagline">
          Global Business Operations & Market Enablement: Latin America & Caribbean.
        </p>
      </div>

      <nav aria-label="Footer navigation" class="footer-nav">
        <h2 class="footer-heading">Navigation</h2>
        <ul role="list">
          {
            NAV_LINKS.map((link) => (
              <li>
                <a href={link.href} class="footer-link">
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <div class="footer-connect">
        <h2 class="footer-heading">Connect</h2>
        <ul role="list" class="social-list">
          <li>
            <a
              href={SOCIAL_LINKS.linkedin}
              class="footer-link"
              target="_blank"
              rel="noopener noreferrer"
            >
              LinkedIn
            </a>
          </li>
          <li>
            <a href={SOCIAL_LINKS.email} class="footer-link">Email</a>
          </li>
        </ul>
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; {currentYear} {SITE.title}. All rights reserved.</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: auto;
    border-top: var(--border-width) solid var(--color-border);
    background-color: var(--color-bg-alt);
  }

  .footer-inner {
    padding-block: var(--space-12);
  }

  .footer-top {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: var(--space-10);
  }

  .footer-logo {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    text-decoration: none;
    color: var(--color-text);
    font-weight: var(--weight-bold);
    font-size: var(--text-lg);
  }

  .footer-logo:hover {
    color: var(--color-primary);
  }

  .logo-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    background-color: #0b1f3a;
    border-radius: var(--radius-sm);
    flex-shrink: 0;
  }

  .footer-tagline {
    margin-top: var(--space-3);
    color: var(--color-text-muted);
    font-size: var(--text-sm);
    max-width: 30ch;
  }

  .footer-heading {
    font-size: var(--text-sm);
    font-weight: var(--weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text);
    margin-bottom: var(--space-4);
  }

  .footer-nav ul,
  .social-list {
    list-style: none;
    padding: 0;
  }

  .footer-nav li + li,
  .social-list li + li {
    margin-top: var(--space-2);
  }

  .footer-link {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: var(--text-sm);
    transition: color var(--transition-fast);
  }

  .footer-link:hover {
    color: var(--color-primary);
  }

  .footer-bottom {
    margin-top: var(--space-10);
    padding-top: var(--space-6);
    border-top: var(--border-width) solid var(--color-border);
    color: var(--color-text-muted);
    font-size: var(--text-sm);
  }

  @media (max-width: 768px) {
    .footer-top {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }
  }
</style>
